# Fichier .gitignore pour un thème WordPress avec Tailwind

# Node modules (TRÈS IMPORTANT - dossier très lourd)
node_modules/

# Fichiers de configuration locaux
.env
.env.local
*.log

# Fichiers système
.DS_Store
Thumbs.db
desktop.ini

# Fichiers de l'éditeur
.vscode/
.idea/
*.sublime-*
*.code-workspace

# Fichiers de build temporaires
*.cache
*.tmp
.temp/

# Sauvegardes
*.backup
*.bak
*~

# WordPress (au cas où)
*.sql
*.zip

# Optionnel : si vous voulez reconstruire le CSS à chaque fois
# style.css
# (décommentez si vous préférez ne pas versionner le CSS compilé)

# FICHIERS SENSIBLES - NE JAMAIS COMMITER !
.env                 # Contient les mots de passe FTP
.env.local          # Toute variante de .env
.env.production     # Toute variante de .env
deploy.js           # Contient le script de déploiement
.vscode/            # Configuration VS Code

# Node modules - Très lourd, jamais sur Git
node_modules/
package-lock.json

# Fichiers système
.DS_Store
Thumbs.db
*.log
*.sql
*.zip